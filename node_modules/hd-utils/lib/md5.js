const crypto = require("crypto")

exports.Get_MD5 = function (str) {
	let md5 = crypto.createHash('md5');
	let sign = md5.update((new Buffer(str)).toString("utf8")).digest('hex').toLocaleLowerCase();
	return sign
}